
if Supports Nvidia-docker-compose:
~ sudo nvidia-docker-compose up/down

for docker-compose use:
~ sudo docker-compose up with the nvidia-docker-compose.yml




For sample:
curl -X POST -F image=@image/location/image.jpg 'http://localhost:5000/predict'
